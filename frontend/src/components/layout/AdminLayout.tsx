import { ReactNode } from 'react';
import { NavLink, useNavigate } from 'react-router-dom';
import { ShieldCheck, Users, Mail, Globe, Activity, FileText, Settings, LogOut, LockKeyhole } from 'lucide-react';
import { useAuth } from '../../context/AuthContext';

export function AdminLayout({ children }: { children: ReactNode }) { return <div className="flex h-screen overflow-hidden bg-[#05080D] text-gray-100 antialiased"><AdminSidebar /><div className="flex min-w-0 flex-1 flex-col overflow-hidden"><header className="flex h-14 shrink-0 items-center justify-between border-b border-[#151D28] bg-[#080D14] px-4 md:px-6"><div><span className="text-[10px] font-bold uppercase tracking-[0.2em] text-red-400">Restricted area</span><span className="ml-3 text-xs text-gray-400">Administrator console</span></div><div className="flex items-center gap-2 text-[10px] font-bold uppercase tracking-wider text-emerald-400"><span className="h-1.5 w-1.5 rounded-full bg-emerald-400 animate-pulse-slow" /> Platform services online</div></header><main className="flex-1 overflow-y-auto p-4 md:p-6 lg:p-8">{children}</main></div></div>; }
function AdminSidebar() { const { user, logout } = useAuth(); const navigate = useNavigate(); const handleLogout = () => { logout(); navigate('/login'); }; return <aside className="hidden w-64 shrink-0 flex-col border-r border-[#151D28] bg-[#080D14] md:flex"><div className="border-b border-[#151D28] px-5 py-5"><div className="flex items-center gap-3"><div className="rounded border border-red-500/20 bg-red-950/30 p-2 text-red-400"><LockKeyhole size={18} /></div><div><h1 className="text-sm font-bold tracking-wide text-white">EMAIL THREAT</h1><p className="text-[9px] font-bold tracking-[0.18em] text-red-400">ADMINISTRATION</p></div></div></div><nav className="flex-1 space-y-1 overflow-y-auto p-3"><p className="mb-2 px-3 text-[9px] font-bold uppercase tracking-[0.18em] text-gray-600">Management</p><NavItem to="/admin" icon={ShieldCheck} name="Admin overview" /><NavItem to="/admin/users" icon={Users} name="Users" /><NavItem to="/admin/scans" icon={Mail} name="Email scans" /><NavItem to="/admin/threat-intelligence" icon={Globe} name="Threat intelligence" /><NavItem to="/admin/system-health" icon={Activity} name="System health" /><NavItem to="/admin/audit-logs" icon={FileText} name="Audit logs" /><p className="mb-2 mt-6 px-3 text-[9px] font-bold uppercase tracking-[0.18em] text-gray-600">Platform</p><NavItem to="/settings" icon={Settings} name="Settings" /></nav><div className="border-t border-[#151D28] p-3"><div className="flex items-center justify-between rounded bg-[#060A10] p-2.5"><div className="flex min-w-0 items-center gap-2.5"><div className="flex h-8 w-8 shrink-0 items-center justify-center rounded bg-red-950 text-sm font-bold text-red-300">{user?.name?.charAt(0) || '?'}</div><div className="min-w-0"><p className="truncate text-xs font-semibold text-gray-200">{user?.name || 'Administrator'}</p><p className="mt-0.5 text-[9px] font-bold uppercase tracking-wider text-red-500">Administrator</p></div></div><button aria-label="Sign out" onClick={handleLogout} className="rounded p-1.5 text-gray-500 transition-colors hover:bg-red-950/40 hover:text-red-400"><LogOut size={15} /></button></div></div></aside>; }
function NavItem({ to, icon: Icon, name }: { to: string; icon: typeof Activity; name: string }) { return <NavLink to={to} end={to === '/admin'} className={({ isActive }) => `group flex items-center gap-3 rounded px-3 py-2 text-xs font-medium transition-colors ${isActive ? 'border border-red-500/20 bg-red-950/30 text-red-200' : 'text-gray-500 hover:bg-[#101722] hover:text-gray-200'}`}><Icon size={15} className="text-gray-600 transition-colors group-hover:text-red-400" />{name}</NavLink>; }
