import { useState, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { getThreats } from '../services/threatApi';
import { Threat } from '../types';
import { SeverityBadge } from '../components/common/SeverityBadge';
import { Search, Filter, ShieldAlert, ArrowUpRight } from 'lucide-react';

export default function Threats() {
  const [threats, setThreats] = useState<Threat[]>([]); const [loading, setLoading] = useState(true); const [searchTerm, setSearchTerm] = useState(''); const [selectedSeverity, setSelectedSeverity] = useState('ALL'); const [selectedType, setSelectedType] = useState('ALL'); const [selectedStatus, setSelectedStatus] = useState('ALL'); const navigate = useNavigate();
  useEffect(() => { getThreats().then(setThreats).finally(() => setLoading(false)); }, []);
  const filtered = threats.filter(t => (!searchTerm || [t.id, t.target, t.location, t.sender, t.type].some(value => value?.toLowerCase().includes(searchTerm.toLowerCase()))) && (selectedSeverity === 'ALL' || t.severity === selectedSeverity) && (selectedType === 'ALL' || t.type === selectedType) && (selectedStatus === 'ALL' || t.status === selectedStatus));
  return <div className="mx-auto max-w-[1600px] space-y-5"><header className="flex flex-col gap-3 border-b border-hairline pb-5"><div className="flex items-center gap-3"><div className="rounded border border-critical/20 bg-critical/10 p-2 text-critical"><ShieldAlert size={19} /></div><div><p className="text-[10px] font-bold uppercase tracking-[0.22em] text-critical">Threat intelligence console</p><h1 className="mt-1 text-2xl font-semibold text-ink">Threat investigations</h1></div></div><p className="text-sm text-ink-mute">Review observed threats and navigate into the underlying investigation record.</p></header>
    <section className="flex flex-col gap-3 rounded border border-hairline bg-raised p-4 xl:flex-row xl:items-center"><div className="relative min-w-0 flex-1"><Search className="absolute left-3 top-2.5 text-ink-faint" size={15} /><input type="search" placeholder="Search threat ID, target, sender, or location" value={searchTerm} onChange={event => setSearchTerm(event.target.value)} className="w-full rounded border border-hairline-strong bg-surface py-2.5 pl-9 pr-3 text-xs text-ink-dim outline-none placeholder:text-ink-faint focus:border-accent/70" /></div><div className="flex flex-wrap items-center gap-2"><Filter size={14} className="text-ink-faint" /><FilterSelect value={selectedSeverity} setValue={setSelectedSeverity} options={['ALL', 'Critical', 'High', 'Medium', 'Low']} label="Severity" /><FilterSelect value={selectedType} setValue={setSelectedType} options={['ALL', 'Credential Theft', 'Phishing', 'BEC', 'Malware', 'Suspicious']} label="Type" /><FilterSelect value={selectedStatus} setValue={setSelectedStatus} options={['ALL', 'Open', 'In Progress', 'Quarantined', 'Resolved']} label="Status" /></div></section>
    <section className="overflow-hidden rounded border border-hairline bg-raised"><div className="flex items-center justify-between border-b border-hairline px-5 py-4"><div><p className="text-[9px] font-bold uppercase tracking-[0.18em] text-ink-faint">Investigation queue</p><p className="mt-1 text-sm text-ink-dim"><span className="font-mono text-accent">{filtered.length}</span> matching records</p></div></div>{loading ? <LoadingRows /> : !filtered.length ? <div className="p-12 text-center font-mono text-xs text-ink-faint">No threat data matches the selected filters.</div> : <div className="overflow-x-auto"><table className="w-full min-w-[980px] text-left text-xs"><thead className="bg-surface text-[10px] uppercase tracking-wider text-ink-faint"><tr><th className="px-5 py-3">Threat</th><th className="px-4 py-3">Classification</th><th className="px-4 py-3">Severity</th><th className="px-4 py-3">Confidence</th><th className="px-4 py-3">Target</th><th className="px-4 py-3">Last seen</th><th className="px-5 py-3">Status</th><th /></tr></thead><tbody>{filtered.map(threat => <tr key={threat.id} onClick={() => navigate(`/threats/${threat.id}`)} className="cursor-pointer border-t border-hairline-strong transition-colors hover:bg-surface"><td className="px-5 py-3"><p className="font-mono font-semibold text-accent">{threat.id}</p><p className="mt-1 max-w-[220px] truncate text-[10px] text-ink-faint">{threat.sender || threat.description || 'No sender context'}</p></td><td className="px-4 py-3 text-ink-dim">{threat.type}</td><td className="px-4 py-3"><SeverityBadge severity={threat.severity} /></td><td className="px-4 py-3 font-mono text-ink-dim">{threat.confidence}%</td><td className="max-w-[180px] truncate px-4 py-3 font-mono text-ink-mute">{threat.target}</td><td className="whitespace-nowrap px-4 py-3 text-ink-mute">{formatDate(threat.lastSeen)}</td><td className="px-5 py-3"><span className="rounded border border-hairline-strong px-2 py-1 text-[10px] font-medium text-ink-mute">{threat.status}</span></td><td className="px-3 py-3 text-ink-faint"><ArrowUpRight size={14} /></td></tr>)}</tbody></table></div>}</section></div>;
}
function FilterSelect({ value, setValue, options, label }: { value: string; setValue: (value: string) => void; options: string[]; label: string }) { return <select aria-label={label} value={value} onChange={event => setValue(event.target.value)} className="rounded border border-hairline-strong bg-surface px-2.5 py-2 text-xs text-ink-mute outline-none focus:border-accent/70">{options.map(option => <option key={option} value={option}>{option === 'ALL' ? `All ${label}` : option}</option>)}</select>; }
function LoadingRows() { return <div className="space-y-2 p-5">{[1, 2, 3, 4].map(row => <div key={row} className="h-12 animate-pulse rounded bg-raised" />)}</div>; }
function formatDate(value: string) { if (!value || value === 'Not reported' || value === 'Unavailable') return 'Not reported by source'; const date = new Date(value); return Number.isNaN(date.getTime()) ? value : date.toLocaleString(); }
